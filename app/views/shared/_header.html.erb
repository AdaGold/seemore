<nav class="navbar navbar-default navbar-fixed-top">
  <div class="container-fluid">
    <div class="navbar-header">
      <a class="navbar-brand nav-logo" href="#">FancyFeed</a>
    </div>
    <ul class="nav navbar-nav navbar-left">
      <li>
        <%= form_tag(search_vimeo_path, :method => "get", class: "navbar-form", id: "search-form") do %>
          <div class="input-append">
            <%= text_field_tag :query, params[:query], class: "span2", placeholder: "Search Hashtags" %>
            <button class="btn" type="submit"><i class="glyphicon glyphicon-search"></i></button>
          </div>
        <% end %>
      </li>
    </ul>
    <% if current_user %>
      <ul class="nav navbar-nav navbar-right">
        <li><%= link_to "Log out", logout_path, method: :delete %></li>
      </ul>
      <ul class="nav navbar-nav navbar-right">
        <li><%= link_to "Settings", user_path(current_user) %></li>
      </ul>
    <% end %>
  </div>
</nav>
